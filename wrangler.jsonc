{
  // This Worker no longer hosts the site — GitHub Pages does
  // (https://kashyap-001.github.io, see .github/workflows/deploy.yml).
  // It stays deployed purely to 301 the old workers.dev URL, which is still
  // printed in CV PDFs already sent to recruiters. See worker.js.
  "name": "kashyap",
  "main": "worker.js",
  "compatibility_date": "2026-07-24"
}
